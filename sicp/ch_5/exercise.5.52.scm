#!/usr/bin/env csi -s

(use test)

;;; Exercise 5.52

;; As a counterpoint to exercise *Note Exercise 5-51::, modify the
;; compiler so that it compiles Scheme procedures into sequences of C
;; instructions. Compile the metacircular evaluator of section *Note
;; 4-1:: to produce a Scheme interpreter written in C.

