
(require 'testes)
(import testes)
(require 'myutils)
(import myutils)

;;; Exercise 2.65:

;; Use the results of *Note Exercise 2-63:: and *Note Exercise 2-64::
;; to give [theta](n) implementations of `union-set' and
;; `intersection-set' for sets implemented as (balanced) binary
;; trees.(5)

;; no
