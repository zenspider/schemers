#lang racket

(require "../lib/testes.rkt")

;; Exercise 2.35

;; Redefine `count-leaves' from section *Note 2-2-2:: as an
;; accumulation:
;;
;;      (define (count-leaves t)
;;        (accumulate <??> <??> (map <??> <??>)))

;; (assert-equal x y)
(done)
